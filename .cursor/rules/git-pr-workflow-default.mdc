---
description: "Default git workflow: feature branch + PR to main (Cursor, Codex, Claude)"
alwaysApply: true
---

# Default Git Workflow: Feature Branch + PR to `main`

For **any** change that touches the repository, assistants (**Cursor**, **Codex**, **Claude / Claude Code**) use this workflow **by default**. Full detail: `docs/CONCURRENT_AGENT_WORKFLOW.md`.

## Required pattern

1. Sync `main`: `git fetch origin`, `git checkout main`, `git pull origin main`.
2. Create a dedicated branch: `git checkout -b agent/<short-slug>` (or `feat/…`, `fix/…`). One branch per task or parallel agent stream.
3. Commit **only** on that branch.
4. Push: `git push -u origin HEAD`.
5. Open a **pull request into `main`** (`gh pr create --base main` with title/body, or GitHub UI).
6. Keep pushing commits to the same branch until **PR CI** (`.github/workflows/ci.yml`) is **green**. For **`agent/*`**, **`feat/*`**, and **`fix/*`**, **`.github/workflows/pr-auto-merge.yml`** enables **squash auto-merge** on the PR (when not a draft); once **`ci_result`** is required on **`main`** and **Allow auto-merge** is on in GitHub repo settings, the PR merges without a manual merge step. If auto-merge cannot be enabled (conflicts, missing settings), fix forward or use `gh pr merge --squash` / leave the **PR URL** for the human. **`.github/workflows/stale-branch-cleanup.yml`** runs on **pushes to `main`**, weekly, and on manual dispatch; it deletes lingering remote heads under those prefixes when the branch had a **merged** PR and has **no open PR**—prefer **Automatically delete head branches** in GitHub settings so cleanup is usually unnecessary.
7. **After the PR is merged:** run **`npm run git:graph-hygiene`** (or at least `git fetch origin --prune`) and delete the finished topic branch locally (`git branch -d agent/...` or `-D` if squash left it non-fast-forward). That keeps Git Graph and remote-tracking refs from stacking stale labels on **`main`**. See `docs/CONCURRENT_AGENT_WORKFLOW.md` (“Keeping the Git graph readable”).
8. **Production closeout** (`AGENTS.md`, `every-chat-commit-deploy-verify-production.mdc`) runs **after** the change is on `main` and Cloudflare deploys have finished—not after the feature-branch push alone. **Do not** treat CI alone as live-site sign-off: `npm run test:homepage` against production may not be runnable in GitHub-hosted runners (bot challenge); run production checks from a capable environment after deploy.

## Exceptions (narrow)

- The user **explicitly** asks to push **directly to `main`** (e.g. emergency hotfix): do that and still complete production closeout after deploy.
- The user **already** has you on a named feature branch for the task: commit there, push, open/update the PR; merge/rebase from `origin/main` if the branch is stale.

## Do not

- Do not treat `git push` to a feature branch as “shipped” for **www.australianrates.com** until the PR is merged to `main` and hosting deploys complete (unless verifying **Pages preview** URLs only).
