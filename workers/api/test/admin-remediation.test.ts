import { describe, it } from 'vitest'

describe('admin remediation lineage diagnostics', () => {
  it.todo(
    'exercise this route against a real lineage remediation fixture or integration environment; the synthetic missing-lineage rows were removed under the real-data-only policy',
  )
})
