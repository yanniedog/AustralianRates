import { describe, it } from 'vitest'

describe('admin diagnostics fetch-event payload endpoint', () => {
  it.todo(
    'exercise this route against a real fetch_events/raw_objects fixture with real R2 payload bytes; the synthetic route-state test was removed under the real-data-only policy',
  )
})
