import { describe, it } from 'vitest'

describe('term deposit product_key identity', () => {
  it.todo(
    'verify term-deposit product_key queries against a real historical term-deposit fixture; the synthetic sqlite product rows were removed under the real-data-only policy',
  )
})
