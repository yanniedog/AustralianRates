import { describe, it } from 'vitest'

describe('queryLogs', () => {
  it.todo(
    'verify log querying against a real global_log fixture or integration database; the synthetic sqlite log rows were removed under the real-data-only policy',
  )
})
