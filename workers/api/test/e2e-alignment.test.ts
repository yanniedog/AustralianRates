import { describe, it } from 'vitest'

describe('e2e alignment API classification', () => {
  it.todo(
    'verify alignment outcomes with real latest-all payload captures and real run-report/fetch-event fixtures; the stubbed fetch responses were removed under the real-data-only policy',
  )
})
