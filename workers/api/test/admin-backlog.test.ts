import { describe, it } from 'vitest'

describe('admin diagnostics backlog endpoint', () => {
  it.todo(
    'exercise this route against a real D1 export fixture or integration worker; the synthetic grouped backlog rows were removed under the real-data-only policy',
  )
})
