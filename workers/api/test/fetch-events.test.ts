import { describe, it } from 'vitest'

describe('persistFetchEvent', () => {
  it.todo(
    'verify fetch-event persistence against a real D1 export fixture and real R2 payload capture path; the synthetic sqlite-based persistence test was removed under the real-data-only policy',
  )
})
