import { describe, it } from 'vitest'

describe('rate-change integrity collisions', () => {
  it.todo(
    'verify collision detection against a real historical term-deposit fixture; the synthetic same-day sqlite rows were removed under the real-data-only policy',
  )
})
