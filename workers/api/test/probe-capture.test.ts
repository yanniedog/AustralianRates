import { describe, it } from 'vitest'

describe('probe payload capture policy', () => {
  it.todo(
    'verify capture policy with real fetch_events/raw_objects fixtures and real R2 writes; the synthetic in-memory probe payload test was removed under the real-data-only policy',
  )
})
